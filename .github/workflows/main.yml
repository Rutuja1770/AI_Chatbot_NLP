<<<<<<< HEAD
name: ai_chatbot_nlp
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - pandas==2.2.2
      - numpy==1.26.4
      - scikit-learn==1.5.1
      - sentence-transformers==2.6.1
      - tensorflow==2.15.0
      - keras==2.15.0
      - tf-keras
      - flask==3.0.3
=======
name: AI Chatbot NLP CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Set up Python
      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # Step 3: Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || pip install -e .

      # Step 4: Run tests (optional)
      - name: Run unit tests
        run: |
          echo "Running chatbot tests..."
          pytest || echo "No tests found, skipping."

      # Step 5: Lint code
      - name: Lint with flake8
        run: |
          pip install flake8
          flake8 . --ignore=E501,W503

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Example for deploying to Render/Heroku/any hosting
      - name: Deploy to Render (example)
        run: |
          echo "Deployment step placeholder. Configure your deployment here."
>>>>>>> ce8f81f (first commit)
